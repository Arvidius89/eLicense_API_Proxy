name: Hardening Check

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hardening:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run hardening check
        run: bash scripts/hardening_check.sh
